# HTTParty | Cucumber

## Bundler
Install o Bundler:

```ruby
$ gem install bundler
```

## Project

>Make the Project Clone
>Run the command to install the dependencies
```ruby
$ bundle install
```
>Run the project using the command
```ruby
$ cucumber
```